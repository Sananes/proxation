import React from 'react'
import styles from './TextBlockGrid.module.scss'
import Section from '../../components/Section'

const ImageText = props => {
  return <Section className={styles.root}></Section>
}

export default ImageText
